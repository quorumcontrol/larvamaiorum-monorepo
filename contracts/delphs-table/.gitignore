node_modules
.env
coverage
coverage.json
#typechain :: for now check-in the typechain because we need it for a netlify build

#Hardhat files
cache
artifacts
deployments/localhost

# files with secrets
bots.ts
bots-*.ts